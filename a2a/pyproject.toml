[project]
name = "blueprint-governed-agent"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "a2a-sdk==1.0.2",
    "uvicorn[standard]",
    "httpx",
    "sse-starlette>=3.3.4",
    "starlette>=1.0.0",
    "protobuf==5.29.5",
]
